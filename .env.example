# Copia este archivo a .env y completa con las credenciales de tu PostgreSQL
# (por ejemplo, una instancia en la nube: Supabase, Neon, Railway, RDS...).
DATABASE_URL=postgresql+psycopg://USUARIO:PASSWORD@HOST:5432/DBNAME?sslmode=require
